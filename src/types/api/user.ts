export type User = {
  first_name: string;
  last_name: string;
  username: string;
  email: string;
  is_verified: boolean;
};

export type Reviewer = {
  id: number;
  first_name: string;
  last_name: string;
};